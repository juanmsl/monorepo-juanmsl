import styled from 'styled-components';

export const InputStateStyle = styled.span`
  font-size: 0.8em;
  text-overflow: ellipsis;
  overflow: hidden;
  -webkit-line-clamp: 2;
  display: -webkit-box;
  -webkit-box-orient: vertical;
`;
