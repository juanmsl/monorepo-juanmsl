
const esES = {
  label: 'common:languages.es',
  locale: 'es'
};

const enUS = {
  label: 'common:languages.en',
  locale: 'en'
};

export const LanguagesMap = {
  esES,
  enUS
};

export const LanguagesList = Object.values(LanguagesMap);
