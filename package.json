{
  "name": "juanmsl",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "portfolio": "turbo dev --filter=portfolio",
    "css-tools": "turbo dev --filter=css-tools",
    "landing-ui": "turbo dev --filter=landing-ui",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "prepare": "husky"
  },
  "devDependencies": {
    "@juanmsl/eslint-config": "*",
    "@juanmsl/tsconfig": "*",
    "husky": "^9.0.11",
    "turbo": "^1.13.0"
  },
  "engines": {
    "node": "^20"
  },
  "packageManager": "yarn@1.22.21",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
