{
  "name": "juanmsl",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "portfolio:dev": "turbo dev --filter=portfolio",
    "css-generators:dev": "turbo dev --filter=css-generators",
    "lint": "turbo lint",
    "lint:fix": "yarn format && turbo lint:fix",
    "format": "prettier --write \"**/*.{ts,tsx,md,js}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "@juanmsl/eslint-config": "*",
    "@juanmsl/tsconfig": "*",
    "husky": "^9.0.11",
    "prettier": "^3.1.1",
    "turbo": "^1.13.0"
  },
  "engines": {
    "node": "^20"
  },
  "packageManager": "yarn@1.22.21",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
